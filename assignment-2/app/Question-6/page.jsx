// Create a functional component called Clock. Use the useState hook to manage a state variable named 
// time initialized to the current time. Use the useEffect hook to update the time state every second to display the current time.

import Clock from "./Clock";

export default function Home(){
    return(
        <Clock />
    )
}