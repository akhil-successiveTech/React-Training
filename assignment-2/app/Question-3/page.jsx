// Create a functional component called ToDoList. Use the useState hook to manage a state variable 
// named todos, initialized as an empty array. Render a list of todos using the map function.

import ToDoList from "../components/ToDoList";

const Home = () => {

    return(
        <ToDoList/>
    )
}

export default Home;