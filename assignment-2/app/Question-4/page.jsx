// Create a functional component called RandomNumberGenerator. Use the useState hook to manage a
// state variable named randomNumber, initialized to a random number between 1 and 100.

import RandomNumberGenerator from "../components/RandomNumberGenerator"

export default function Home(){
    return(
        <RandomNumberGenerator/>
    )
}